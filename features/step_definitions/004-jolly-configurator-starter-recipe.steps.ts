// Step definitions for feature 004: Jolly Configurator starter recipe.
import { Given, When, Then } from "@cucumber/cucumber";
import type { JollyWorld } from "../support/world.ts";

// ── Prepare starter recipe ───────────────────────────────────────────────

Given("the customer has created or selected a Saleor Cloud environment", function (this: JollyWorld) {
  // Contract.
});

When("Jolly prepares the initial store configuration", function (this: JollyWorld) {
  // Contract.
});

Then("it should use a Jolly-specific starter recipe", function (this: JollyWorld) {
  // Contract.
});

Then("the recipe should be optimized for Paper's required storefront features", function (this: JollyWorld) {
  // Contract.
});

Then("the recipe should be written into the cloned storefront repository", function (this: JollyWorld) {
  // Contract.
});

Then("the recipe should be reviewable before deployment", function (this: JollyWorld) {
  // Contract.
});

Then("the recipe should be deployed through the safe Configurator workflow", function (this: JollyWorld) {
  // Contract.
});

Then("the Saleor app token used for deployment should have all available permissions in v1", function (this: JollyWorld) {
  // Contract.
});

// ── Apply starter recipe safely ──────────────────────────────────────────

Given("the Jolly starter recipe is ready", function (this: JollyWorld) {
  // Contract.
});

When("the agent applies it to Saleor Cloud", function (this: JollyWorld) {
  // Contract.
});

Then("it should validate the configuration", function (this: JollyWorld) {
  // Contract.
});

Then("it should show a diff or deployment plan", function (this: JollyWorld) {
  // Contract.
});

Then("Jolly remote\\/action commands involved in recipe deployment should support `--dry-run` preview behavior", function (this: JollyWorld) {
  // Contract.
});

Then("the customer's agent should decide whether customer approval is needed before applying changes", function (this: JollyWorld) {
  // Contract.
});

Then("it should fail safely if destructive or breaking operations are detected", function (this: JollyWorld) {
  // Contract.
});
