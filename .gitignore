node_modules
dist/
.env
# .env sidecars and backups carry the same live credentials as .env itself.
.env.*
.mcp.json
bun.lockb
.claude/
.agents/
.agent/
.pi/
skills-lock.json
.vercel
.sandbox-rerun.txt

# c8 coverage output (transient verification artifact)
coverage/
