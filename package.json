{
  "name": "@dk/jolly",
  "version": "0.9.6",
  "description": "Ahoy, agent. Go build a Saleor storefront.",
  "type": "module",
  "bin": {
    "jolly": "bin/jolly"
  },
  "files": [
    "bin/",
    "dist/",
    "assets/skills/",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node20.12 --outfile=dist/index.js",
    "prepack": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node20.12 --outfile=dist/index.js",
    "prepublishOnly": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node20.12 --outfile=dist/index.js",
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "test": "node --test \"tests/**/*.test.ts\"",
    "test:bdd": "cucumber-js",
    "test:logic": "cucumber-js -p logic",
    "test:sandbox": "cucumber-js -p sandbox",
    "test:eval": "cucumber-js -p eval",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "@earendil-works/pi-coding-agent": "^0.79.1",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "happy-dom": "^15.11.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@bomb.sh/args": "^0.3.1",
    "@bomb.sh/tab": "^0.0.17",
    "@clack/prompts": "^1.6.0"
  }
}
