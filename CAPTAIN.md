<!-- ============================================================= -->
<!-- STOP. CAPTAIN ROLE ONLY.                                      -->
<!-- If you are NOT running as the Captain — i.e. you are the      -->
<!-- Quartermaster, Crew Mate, Bosun, or any other role — do NOT   -->
<!-- read past this line. Close this file now. Its contents are    -->
<!-- Captain-only working context and must never enter another     -->
<!-- role's context. You were not given this file by your role.    -->
<!-- ============================================================= -->

> **STOP — CAPTAIN ROLE ONLY.** If you are not running as the Captain (you are the Quartermaster, Crew Mate, Bosun, or any other role), **stop reading now and close this file.** Nothing here is input to your role; reading it leaks Captain-only context. Binding behavior lives in `features/*.feature` and referenced `assets/**` — not here.

# Captain Notes

Captain-only notes: product framing, rationale, open questions, and current state. **Non-binding.**

Only Captain may read or edit this file. QM, Crew, and Bosun must not. Binding behavior lives in `features/*.feature` and referenced `assets/**`, never here.

> **Captain authors every `.feature` scenario** — read and follow `SCENARIO_WRITING.md` (the scenario-writing guide) for each one.

## GOVERNING DESIGN DECISION (dk, 2026-06-15): live-by-design, never mock/fake

**Binding intent.** Jolly's test suite runs against **real services** in a **fully integrated test env that matches production** — the `JOLLY_*` Saleor Cloud / Vercel / Stripe credentials in `.env` ARE that test env. **Never mock or fake** (no fake Stripe/configurator CLIs, no dummy credentials, no `.invalid` endpoints, no simulated responses). **Creating real resources is expected and correct** — that is the point. Safety is **harmless-by-design = namespace every created resource + idempotent teardown + never modify/delete a resource the run did not create** (the existing AGENTS.md rules) — NOT credential-faking. This **supersedes the "012 incident" forced-safe approach** (`logicSafeEnv`/dummy creds): the guard against 012-class harm is the never-touch-what-we-didn't-create rule, not dummies. Scope: **every tier, including `@logic`** — no mock/fake anywhere.

**What this rewrites:**
- **Methodology (AGENTS.md):** "Prefer sandbox over mocks" → "real services always; mocks/fakes forbidden; harmless-by-design = namespace+cleanup." (AGENTS.md is the home of test methodology; flagged for dk's sign-off per the Captain/AGENTS.md boundary.)
- **Spec (feature 025):** the `@eval` "Harmless by design — bounded, no real cloud" rule and its "no real cloud resource created / nothing deployed" assertions INVERT — the eval drives the agent against the real test env and verifies the live result (real namespaced store/deploy/Stripe-test-mode), with teardown.
- **Harness (QM cycle):** rip out `logic-env.ts` (`logicSafeEnv`/`DUMMY`/`.invalid`) from ~19 step files + `dotenv`/`eval`; delete `stripe-cli-fake.ts` + `configurator-cli-fake.ts`; rewire every tier to the real `.env` services with namespace+teardown.

**Operational realities to honor (not optional):**
- **Env limits — cannon fodder (dk):** environments in this test org are disposable. When the org limit is hit (`ENVIRONMENT_LIMIT_REACHED`), the harness reclaims capacity by DELETING `jolly-test`-namespaced environments and proceeds — it is NOT a skip. The `jolly-test-` prefix (`features/support/sandbox.ts:293`, `makeNamespace` → `jolly-test-${runId}`) IS the protection boundary: only `jolly-test-*` envs are deletable cannon fodder; the configured `jolly-store` (the `.env` endpoint) and any future non-test env are never deleted. Sharing one per-run env is allowed as efficiency, not required.
- **Vercel:** live deploy needs a one-time interactive `vercel login` (browser) on the runner; absent it, deploy-touching tests skip-not-fail (capability gate), as `@sandbox` already does for `vercel whoami`.
- **Stripe:** real `@stripe/cli` + real test-mode keys; test cards only (worst case = declined, never a real charge).

**Failure-condition production policy (dk decided 2026-06-15): real where possible; narrow justified double otherwise.**
- **Produce for real** (the majority): every failure reachable from real bad input — empty/garbage token → real auth rejection; non-first-party `--url` → real `NON_FIRST_PARTY_HOST` pre-flight refusal; malformed input → real honest error. No double.
- **Justified-exception double** (enumerated, inline-justified, never the normal path) only for conditions the real test env genuinely cannot produce on demand. Current set: `ENVIRONMENT_LIMIT_REACHED` (org at its env limit) and the **unverifiable-endpoint** "stored, not verified" path (a deliberately unreachable service). If QM finds a real way to trigger these cheaply (e.g. fill-then-reclaim cannon-fodder envs), prefer that.
- **"No fabricated pass" doctor checks (020/014/021/024…):** under real creds these checks now legitimately PASS — so re-aim the assertion at its real intent: real reachable store → assert real `pass`; the never-fabricate invariant is proven by feeding **real junk/bad input** and asserting no success/verify language, not by an unroutable endpoint.

## DESIGN DECISION (dk, 2026-06-16): browser OAuth is URL-first; strip Playwright; all CLIs via npx

**What prompted it.** A directed `/qm` pass reported the worklist green but with skips; dk questioned them. Investigation (this session) showed the skips were NOT missing credentials — all `JOLLY_*` creds present, Vercel CLI authenticated (`npx vercel whoami` exit 0; the one logic "skip" is the exceptional *Doctor reads the Vercel CLI login state* scenario that can't fabricate a "not logged in" precondition), Stripe driven via npx. The only real capability skips were the browser-OAuth scenario (no Playwright/native browser) and the deployed-storefront-URL scenario. dk: the Playwright tiering was overreach.

**Decision 1 — Browser OAuth is URL-first, like other CLIs (feature 018 rewritten).** `jolly login` / `jolly login --browser` generate the Keycloak authorization URL, **print it for click/copy-paste**, start the localhost callback server, and open a browser **when one is available** (convenience only). A missing browser is **never an error** — the printed URL is the always-available path; `--token` stays the fully non-interactive path. **Purged entirely** (runtime + harness): the native→Playwright→token tier ladder, Playwright detection, the `@requires-browser` tag/gate, the `HARNESS_SALEOR_EMAIL/PASSWORD` knobs, and the stdin email/password prompting. Jolly now never sees/holds the user's credentials at all (human types them into their own browser). The perpetually-skipped end-to-end *human-consent* scenario was **replaced** by a deterministic `@logic` scenario asserting the URL-presentation + open-when-available + no-error-when-headless contract (runs everywhere, no skip). Human consent round-trip is a manual check, not CI.
- **Orphaned for QM/Bosun cleanup:** `features/support/browser.ts` (whole file), the `@requires-browser` Before hook + imports in `hooks.ts`, the `"Agent completes the full browser OAuth login flow"` entry in `sandbox.ts` `SANDBOX_REQUIREMENTS`, and any step defs for the removed scenario. The new `@logic` scenario's 4 Then steps are undefined → QM makes them executable.

**Decision 2 — All CLIs via npx (tooling).** dk: configurator, vercel, and stripe are all used via `npx`; none need a global install, and a missing global binary is NOT a failure. Already true in code (`sandbox.ts` uses `npx vercel`); saved to agent memory (`clis-via-npx`). **dk directed it into AGENTS.md** (Runtime and Build) as an explicit tooling rule so Crew sees it when writing `src/` — added with dk's authorization (the Captain/AGENTS.md boundary forbids *unilateral* AGENTS.md edits during spec work, not human-directed ones).

## Current state / next outbound

- **WHOLE-FLOW NOW WORKS END-TO-END — auto-provision + Bug B + Bug C all fixed; a baseline eval agent (pi) drove the full flow autonomously (dk-directed, 2026-06-17; committed locally `a2ae9bd`, `b0277d8`, `0814edb`; outbound pending dk).** dk asked to use the eval to provision a store+dashboard and see pi do it. Delivered the entire chain:
  - **Auto-provision store stage (feature 002, blocker #1 IMPL):** `jolly start`'s store stage was `pending`; now `runStoreStage` auto-provisions a Saleor Cloud env when a Cloud token is set but no endpoint (shared `provisionStore()` extracted from `commandCreateStore`), writes endpoint+app-token to `.env`+process.env, surfaces `data.store.{graphqlApiUrl,dashboardUrl}`. Honors `JOLLY_STORE_NAME`/`JOLLY_STORE_DOMAIN_LABEL` config (default `jolly-store`); the @sandbox harness sets it to the per-run `jolly-test-<run>` namespace (002 steps). **`@sandbox` 002:41 passes end-to-end.** This resolves the namespacing fork (blocker #1) via a real config affordance, not test knowledge in prod.
  - **Bug B (recipe stage) — three real layers, all fixed:** (1) the configurator binary's flag is `--failOnDelete` (camelCase), NOT `--fail-on-delete`; `--fail-on-breaking` does not exist in the binary (its README documents it; latest is 3.23.1). (2) A fresh Saleor store ships 8 stock defaults the recipe deletes, so `--failOnDelete` blocks the bootstrap → on the **bootstrap path (store provisioned this run) deploy WITHOUT the guard**; keep `--failOnDelete` for re-deploys over a pre-existing store (exit 6 → blocked). (3) the configurator requires `--config` WITHIN its cwd, so copy the bundled recipe into the project dir first; and the bootstrap apply exits 5 ("partial") while replacing protected defaults even though its report records success → **trust the configurator's deployment-report `summary.status`, not the exit code.** Respec'd feature 004 (+003/012 prose) to match tool reality. dk decision: "allow deletes on bootstrap."
  - **Bug C (Vercel deploy):** the deploy spawned `vercel deploy` with no env, so the Paper Next.js build failed. Now passes `--build-env NEXT_PUBLIC_SALEOR_API_URL=<endpoint> --build-env NEXT_PUBLIC_DEFAULT_CHANNEL=us` and captures the real `*.vercel.app` URL into `data.deploy`. Verified: a live `jolly start` deploy completes + captures the URL.
  - **Eval made deploy-capable + drove the whole flow (feature 025):** the eval ran pi under an isolated `$HOME` with no Vercel session → deploy gated. Fix: `passThroughVercelSession` copies the runner's real Vercel CLI session into pi's fake `$HOME` (live-by-design; skip-not-fail absent). Implemented the previously-UNDEFINED 025 URL step (conditional on stage completion; recurses into `data.store`/`data.deploy`). Added a `jolly start --yes` autonomous-setup note to `assets/homepage/setup.md`. **Result (HARNESS_EVAL_SETUP_LOCAL=1 + keep-store):** pi read the guide, chose `--yes` on its own, and ran `jolly start --yes` to **all 8 stages `completed`** — provisioned `https://jolly-test-eval-1781711002.saleor.cloud/dashboard/` (11 products) and deployed `https://storefront-g3s9zaxgt-dmytri-kleiners-projects.vercel.app` (401 = Vercel Deployment Protection until disabled). `-p eval` scenario PASSES.
  - **Verification:** `-p logic` 98 passed / 1 skip / 0 failed; typecheck clean; discovery 0 undefined (default + eval); `@sandbox` 002:41 green; eval green. Two live `jolly-test` envs remain (cannon fodder): the manual `jolly-test-keeper-1781705348` and pi's `jolly-test-eval-1781711002`.
  - **002:58 WHOLE-FLOW GATE NOW GREEN (committed `7f65de5`):** after a successful deploy the deploy stage probes the Saleor endpoint and pushes a `deployed-storefront-saleor-connectivity` check (read-only; pass only on a real GraphQL response) + a `saleor-trusted-origin` guided step (no first-party Cloud API registers a storefront trusted origin in v1 — Saleor Cloud allows anonymous storefront reads without it; 002:106 reworded). `@sandbox` 002:58 passes end-to-end (all stages completed, env `jolly-test`-namespaced, dashboard + storefront URLs, connectivity check). NOTE: a transient `fetch failed` in the test's `listAllEnvironments` verification failed the FIRST run; re-run was green — Cloud-API transient, not a defect ([[logic-parallel-loopback-flakiness]] pattern).
  - **STILL PENDING (honest record):** (a) **Vercel project namespacing**: deploys create a Vercel project named `storefront` (dir name), NOT `jolly-test-*`, and neither the eval nor 002:58 registers Vercel teardown — it leaks ([[jolly-test-namespace-all-test-runs]]). (b) the eval used `HARNESS_EVAL_SETUP_LOCAL=1` (local guide) + `JOLLY_STORE_NAME` via env; the canonical run points at live `jolly.cool/setup` (needs the setup.md change deployed) and a store-name hook for the bare canonical task. (c) pi's per-command timeout vs the ~8-min `jolly start --yes`: it completed here, but a slower run could hit pi's bash budget — the guide now suggests background+poll. (d) the run still reports `status: warning` overall (the human Stripe-Dashboard-app gate legitimately remains).

- **QM CYCLE 2026-06-17 — BLOCKER #2 EXECUTED (feature 008:72 `.env`-read observable now live; committed locally `6f5e0aa`, main 9 ahead of origin; outbound pending dk).** The single undefined target the strengthened 008 scenario surfaced (blocker #2 below) is now executable end-to-end via QM→Crew→Bosun: QM made the new Then step executable (asserts `data.cloudTokenSource` references `.env`, and NOT process/environment, so the incidental `.env` in riskContext sideEffects can't pass it trivially); Crew implemented `commandCreateAppToken`'s dry-run preview to report `cloudTokenSource` (`"project .env"` when the token is in the `.env` file, `"process environment"` on fallback, else `"unresolved"`) — `src/index.ts`. Verified fresh: `--dry-run` 0 undefined; `008:72` 9/9; typecheck clean; `-p logic` 98 passed / 1 cred-gated skip / 0 failed. This closes the title-vs-steps green-suite-blindness flagged as MINOR at line ~58: the dry-run now positively proves it read the `.env` token. **Blockers #1 (auto-provision namespacing fork) and #3 (parallel-logic flakiness, triaged) below are UNCHANGED — #1 still needs the auto-provision impl cycle.**

- **QM BLOCKERS RESOLVED 2026-06-17 (Captain, durable artifacts updated; clear → `/qm` to execute).** The three blockers QM left at the end of its pass are resolved into durable spec/notes so the next QM cycle proceeds without hidden context:
  1. **Namespacing fork (scenario 5) — RESOLVED in spec.** New feature 002 Rule **"Auto-provisioning a store, and how the store is named"**: `jolly start`'s store stage provisions a Saleor Cloud environment itself when a Cloud token is set but no endpoint, writes the endpoint + app token to `.env`, and names the store from an **optional configured store name** (the same name/domain-label override `create store` accepts, surfaced via project config) with a sensible default otherwise. That override is an ordinary customer affordance AND the single hook the harness uses to make provisioned stores `jolly-test-<run>` cannon fodder (exactly as `provision.ts` passes `--name`/`--domain-label` to `create store`) — no test knowledge baked into production. *Next QM/Crew:* build the auto-provision store stage honoring the name override; the `@sandbox` harness sets that configured name to the per-run `jolly-test` namespace for scenarios 3/5 (so the snapshot-diff teardown reclaims it). This unblocks the auto-provision implementation cycle.
  2. **Scenario 8 weak coverage — RESOLVED by strengthening the spec (spec-ahead delta).** Added one observable to feature 008's app-token `.env`-read scenario: **"the preview should report that the Cloud token it would authenticate with was read from the project `.env`"** — a positive, falsifiable signal that the dry-run actually READ the `.env` token (it currently returns early without touching it), closing the title-vs-steps green-suite-blindness. `--dry-run` now surfaces exactly **1 undefined step** as the next QM target; Crew makes `create app-token --dry-run` resolve + report the `.env` Cloud-token source (referenced by name, never printed).
  3. **`-p logic` parallelism flakiness — RESOLVED as triaged-and-mitigated (not a product/spec defect).** The full serial `-p logic` run is green (98 passed / 1 cred-gated skip / 0 failed); the flake is parallel-only loopback-server contention. Mitigation recorded to memory ([[logic-parallel-loopback-flakiness]]): a `-p logic` failure must be re-run in isolation before being treated as a defect. Optional future harness-hardening (mirror the 018 cross-process file-lock pattern, see entry ~line 90) is deferred — not pursued unless it actually starts blocking CI.

- **QM CYCLE RAN 2026-06-17 — 8 SCENARIOS MADE EXECUTABLE; BUG A + OAUTH AUTH-GATE SHIPPED (committed locally `1f0640f`, main 5 ahead of origin; outbound pending dk).** The QM→Crew→Bosun cycle worked the 8 undefined targets from the ★ handoff below. **Delivered & green:** (1) **Bug A FIXED** — `create store` AND `create app-token` now read `JOLLY_SALEOR_CLOUD_TOKEN` `.env`-first via `loadEnvValues(projectDir())` (fallback `process.env`), so provisioning works for a real agent whose token lives only in `.env` (`src/index.ts:1142`/`1454`). (2) **OAuth auth-gate FIXED** — `jolly start` with no Cloud token now presents the Keycloak browser-login URL as a NON-blocking gate (auth stage no longer fabricates `completed`; not an error on missing token/browser; `nextSteps` offers browser login or `jolly login --token`). (3) **Executable coverage for all 8** — `@logic` scenarios 1/2/4/7/8 PASS; `@sandbox` 3/5/6 (auto-provision, whole-flow e2e gate, `stripe login` passthrough) are DEFINED + gated in `SANDBOX_REQUIREMENTS`/`VERCEL_CLI_SCENARIOS` and skip locally. Verified fresh: typecheck clean; `--dry-run` 0 undefined; `-p logic` 98 passed / 1 cred-gated skip / 0 failed. Bosun removed one orphaned step def (`a fresh project directory with no store URL configured`).
  - **STILL PENDING in the ★ worklist (the `@sandbox` IMPLEMENTATIONS — the whole-flow e2e gate does NOT yet pass):** the `jolly start` store stage still returns `pending` under `--yes` (no auto-provision yet); the deploy chain (Bug B configurator flags, Bug C Vercel build) is untouched; the harness `.env`→`process.env` export (root-cause methodology fix, `world.ts`/`dotenv.ts`) was NOT done (QM derives its worklist from undefined/failing scenarios, and the new `@logic` `.env`-read scenarios 7/8/2 prove the credential path per-scenario via env overrides, so they're valid regardless — but the GLOBAL export still masks the bug class for other tests). Secret-min + eval observability/namespacing also still pending.
  - **OPEN SPEC FORK surfaced by QM (needs dk before the auto-provision impl cycle):** scenario 5 asserts "the created Saleor environment should be `jolly-test`-namespaced", but bare `jolly start --yes` takes no `--name` — so how does `start`'s auto-provision learn the test namespace? For a real customer `start` should name the store sensibly (NOT `jolly-test`). Candidates: (a) the `@sandbox` harness injects the namespace via a `HARNESS_*` knob `start`'s auto-provision respects (consistent with provision.ts passing `--name jolly-test-*` to `create store`); (b) `start` self-derives a name and the test asserts a configurable prefix. This is the same namespacing-impl item already flagged below (line ~56/64/70). **Decision-dependent — flagged, not specced, to avoid a contradiction.**
  - **MINOR (honest record):** scenario 8 (`create app-token --dry-run` reads `.env`) passes on its literal steps (success + names the GraphQL app-token request + no side effects), but the dry-run returns early WITHOUT exercising the Cloud token, so the scenario's TITLE intent (".env read") isn't strongly exercised. Faithful to the steps as written; flag for spec refinement if stronger coverage is wanted. Also recorded a reusable harness gotcha to memory: a `-p logic` failure may be a loopback-server race under parallelism — re-run serially before treating it as a defect ([[logic-parallel-loopback-flakiness]]).
  - **OUTBOUND:** finish-first hold was already lifted (dk 2026-06-17, see below); `1f0640f` is a coherent green increment (Bug A + auth gate + coverage), safe to push. dk to confirm push of the 5-ahead `main`, OR hold to bundle with the auto-provision/deploy implementation (the e2e gate is defined but not yet passing). Next cycle (after dk resolves the namespacing fork): clear → `/qm` for the auto-provision store stage + Bug B/C.

- **★ QM HANDOFF 2026-06-17 — "PROVE A BASELINE AGENT CAN DO THE WHOLE FLOW" (specs written; next session: clear → `/qm`).** The eval showed the affordance works but the live flow never completes; driving it by hand proved the flow is broken in 3 places and the green suite was structurally blind to it. Root cause: the harness exports `.env` into Jolly's `process.env` (`features/support/dotenv.ts` + `world.ts:94` spreads `process.env` into every spawned Jolly), so Jolly's `process.env`-first credential reads work in tests but fail for a real user whose creds live ONLY in `.env`. Specs added this session drive the fix; the worklist below is the QM order.
  - **NON-NEGOTIABLE CONSTRAINTS (all dk, 2026-06-17):** (1) **no mocks/fakes/doubles** except `@exceptional-double` (AGENTS.md + feature 026 `@property` enforce it). (2) **Upstream-CLI-first / minimize stored secrets** — Jolly stores no secret and calls no provider API it can delegate to an official CLI under that CLI's own auth; every `.env` secret is leak surface ([[upstream-cli-first-minimize-secrets]]). (3) **`jolly-test` namespace for ALL test-run resources** (Saleor envs AND Vercel projects), so cleanup reclaims them as cannon fodder ([[jolly-test-namespace-all-test-runs]]). (4) **CLIs via `npx`**, install only if npx is unsupported ([[clis-via-npx]]). (5) **Test the real way** — run Jolly with creds in `.env` only (clean `process.env`).
  - **HARNESS FIX (root cause — QM, do this first):** stop exporting `.env`→`process.env` for spawned Jolly (`world.ts`/`dotenv.ts`); present credentials via the project `.env` FILE only, the way a real agent leaves them. This unmasks the whole bug class. (Methodology lives in AGENTS.md — flag for dk if an AGENTS.md edit is wanted.)
  - **NEW SPEC SCENARIOS (8 undefined targets, all parse clean):** `002` — `jolly start` auto-provisions when no endpoint (`@sandbox` + `@logic` dry-run), OAuth URL-first when no token (`@logic`), and ONE true end-to-end gate "one jolly start drives the whole flow from a real agent's starting state" (`@sandbox`, creds in `.env` only → all stages completed + dashboard + storefront URL). `008` — `create store`/`app-token` read the token from `.env` not the exported env (`@logic` ×2, Bug A). `005` — `jolly start` runs `stripe login` interactively when not authed (`@sandbox`). `002:16` Given tightened (authenticated precondition) so it no longer contradicts the OAuth-first flow.
  - **PRODUCT BUGS the failing tests force (Crew):** **A** — all credential reads via `loadEnvValues(.env)`, never `process.env`-first (`src/index.ts:1142` create-store; `runRecipeStage`). **B** — recipe stage's `@saleor/configurator deploy` rejects `--fail-on-delete` on the npx-resolved v3.23.1; fix by **pinning a working version via `npx @saleor/configurator@<ver>`** (not a global install). DECISION/CONFLICT: feature `004:16` mandates `--fail-on-delete`/`--fail-on-breaking` as safety guards — keep them (pin a version that supports them), do not silently drop. **C** — `npx vercel deploy` fails at the build step (cause truncated in the envelope); diagnose in isolation (likely a missing Vercel build env var).
  - **WHOLE-FLOW AUTH GATES (jolly start drives them; human completes):** no Saleor token → URL-first OAuth (feature 018); Vercel not authed → `vercel login` stdio-passthrough (already `002:101`); Stripe not authed → `stripe login` stdio-passthrough (new `005`). These are the irreducible human gates; everything else is agent-automated.
  - **DEFERRED RECONCILIATIONS (need a deliberate cycle; flagged, not yet specced to avoid contradictions):** **secret-minimization** — stop writing the Stripe SECRET key to `.env` (Stripe CLI/Dashboard holds it); this REQUIRES reconciling `005:21`/`005:46` (which currently write `JOLLY_STRIPE_SECRET_KEY`) — confirm with dk, then Captain updates 005 + a no-`sk_test_`-in-`.env` check. **Observability** — `persistEvalTranscript` (`features/support/eval.ts`) must scrub ALL tracked secrets (not just `HARNESS_OPENROUTER_API_KEY`) AND persist pi's session JSONL (currently dropped; `agent.stdout` was empty) so we can SEE what pi did. **Namespacing impl** — `025:131` already requires `jolly-test`-namespaced agent resources, but agent-driven `jolly create store` defaults to `jolly-store`; the eval harness must inject `--name jolly-test-<runid>` (and name Vercel projects `jolly-test-*`).
  - **CLEAN-MVP DISCIPLINE:** behavior changes REPLACE old scenarios (not append); Crew refactors the affected module to current specs only (don't branch beside old logic); Bosun now removes unreachable `src/` (AGENTS.md updated, pending upstream to Shipshape) + a dead-code/coverage sweep gate.
  - **WORKLIST ORDER:** harness fix → e2e gate + Bug A → Bug B (pin configurator) → Bug C (deploy) → keystone auto-provision + OAuth/stripe-login gates → secret-min + eval observability/namespacing.
- **AD-HOC E2E INVESTIGATION 2026-06-17 — THE WHOLE-FLOW IS BROKEN IN 3 PLACES + 4 HARNESS/AFFORDANCE GAPS (dk goal: "prove agents like pi can do the whole flow"; dk had me run it ad-hoc myself to learn, then spec for QM).** Cleaned both platforms to zero (deleted Saleor `jolly-store` + Vercel `storefront`/`jolly-acceptance`; kept `homepage`=jolly.cool + dk's unrelated projects), then drove the flow by hand (`node bin/jolly …` directly, not via pi, so stdout was capturable). Ground truth — the flow does NOT complete even driven directly:
  - **BUG A (provisioning blind to `.env`) — `src/index.ts:1142`.** `jolly create store --create-environment` reads the cloud token from `process.env["JOLLY_SALEOR_CLOUD_TOKEN"]`, while every other command (doctor/auth/start) reads the `.env` FILE via `loadEnvValues(projectDir())`. Jolly's CLI never loads `.env` into `process.env`. So with the token in `.env` but not exported (the NORMAL state after `jolly login` writes it there), provisioning dies `MISSING_CLOUD_TOKEN` even though `jolly start`'s doctor shows the token present in the same dir. **This is the dead-end that stops the agent flow.** Fix: read the token (and region/org/name overrides?) via `loadEnvValues`, consistent with the rest of the CLI. Workaround used: `set -a; . .env` then provisioning succeeded → real env created, `https://jolly-store.saleor.cloud/dashboard/`, app token written.
  - **BUG B (recipe stage flag mismatch) — recipe stage.** `jolly start` spawns `npx @saleor/configurator deploy … --fail-on-delete --fail-on-breaking`; the resolved configurator (3.23.1) rejects it: `error: unknown option '--fail-on-delete'` → `recipe-deployed: fail`, and `stock-seeded: fail` cascades. (The configurator `deploy --help` EXAMPLES show `--fail-on-delete` but the binary rejects it — a configurator version/inconsistency.) Decision needed: drop/guard the flags, or pin the configurator version Jolly drives.
  - **BUG C (deploy fails at build) — deploy stage.** `npx vercel deploy --prod` reaches Vercel's build, installs deps, then exits 1 (envelope truncates the tail). Likely the storefront build needs `NEXT_PUBLIC_SALEOR_API_URL` as a Vercel env var and/or a build error. Needs a fresh isolated diagnosis (QM/Crew). `stripe-app-installed` PASSED (appInstall works).
  - **AFFORDANCE GAP (the reason pi can't do it):** the agent ran plain `jolly create store --json --yes` (no `--create-environment`) → no-op guidance, provisioned nothing. The skill/setup guide must lead the agent to `jolly create store --create-environment` — OR `jolly start` should itself run provisioning when no endpoint is configured (store stage is currently `pending`, never auto-executed). **Top design fork (needs dk):** auto-provision inside `jolly start` vs keep explicit + fix the skill affordance.
  - **NAMESPACING (dk 2026-06-17 "use jolly-test for ALL test run namespaces, not just jolly, so we delete them as cannon fodder"):** default `jolly create store` names the env `jolly-store` (`src` `effectiveName = name ?? "jolly-store"`) and the Vercel project `storefront` — NEITHER carries the `jolly-test` cannon-fodder prefix, so test runs leak. The eval/harness (feature 025) must inject `--name jolly-test-<runid>`/`--domain-label` and name Vercel projects `jolly-test-*`. Saved to memory [[jolly-test-namespace-all-test-runs]].
  - **OBSERVABILITY:** (a) `jolly start` emits ONE buffered envelope at the end — no streaming stage progress (even direct, stages are only visible via the process tree). (b) the eval doesn't persist pi's real transcript (its session JSONL under the throwaway `$HOME`); `agent.stdout.txt` was empty. To "see what pi is doing" the eval must persist (scrubbed) the pi session log, and/or `jolly start` should stream stage events. dk flagged this as a good FIRST spec.
  - **PROPOSED QM ORDER (after dk resolves the forks):** crisp/decision-free first → BUG A (`.env` read), secret-scrub + pi-session-transcript persistence (feature 025), jolly-test namespacing. Then design-dependent → provisioning entry (fork), BUG B (configurator flags), BUG C (deploy diagnosis), agent-budget/streaming. Each: QM writes the failing scenario, Crew fixes `src`/harness, Bosun commits.
- **EVAL RUN 2026-06-17 — ISSUES FOUND (spec-cleanup worklist; dk: "remember to spec clean up for any issues").** A live `@eval` run (`-p eval`, `HARNESS_EVAL_KEEP_STORE=1` + `HARNESS_EVAL_TRANSCRIPT_DIR`, endpoint forced empty for fresh provisioning) surfaced concrete gaps to clean up via proper QM/Captain follow-up, NOT leave as observations:
  1. **SECRET-SCRUB GAP (feature 025 / `persistEvalTranscript` in `features/support/eval.ts`).** Transcript keeping scrubs ONLY `HARNESS_OPENROUTER_API_KEY`. But the agent `cat`s its workspace `.env`, so the real `JOLLY_SALEOR_CLOUD_TOKEN` / `JOLLY_STRIPE_*` land in the pi session log AND in the persisted `env` file written to `HARNESS_EVAL_TRANSCRIPT_DIR/<ns>/` — UNREDACTED. The kept transcript leaks live credentials. Fix: scrub all tracked secrets (the `JOLLY_*`/Stripe values), not just the OpenRouter key, from every persisted artifact (and ideally the seeded-`.env` copy). Needs an executable assertion (a `@logic`/`@eval` check that no seeded secret value appears in the persisted transcript).
  2. **FRESH-STORE CONTRADICTION (feature 025 vs harness).** Feature 025 says the store endpoint is "left unset so `jolly start` provisions a fresh `jolly-test` store", but `dotenv.ts` loads the whole repo `.env` into `process.env` and `realEnvFileContents()` seeds `NEXT_PUBLIC_SALEOR_API_URL`/`JOLLY_SALEOR_APP_TOKEN` whenever present — so by DEFAULT the agent reuses the existing `.env` store (mutating it) and `HARNESS_EVAL_KEEP_STORE` is moot. The spec and the harness disagree. Decide the intended behavior and make ONE true: either the eval must strip the endpoint/app-token from the seeded workspace `.env` (realize the spec), or the spec text must stop claiming fresh provisioning. (This run forced the endpoint empty as a workaround.)
  3. **NAMESPACE CLAIM TO VERIFY.** Feature 025 asserts every created env is `jolly-test`-namespaced, but the agent runs bare `jolly start --yes` (no `--name` override like the @sandbox harness passes), so the env name is whatever `jolly start` defaults to — confirm it actually carries the `jolly-test` prefix, else the "only `jolly-test-*` is deletable cannon fodder" safety boundary AND the eval's own cleanup both silently miss it (leak). [store name pending run completion]
  - **NOT a Jolly issue:** the hard-coded `storefront1.saleor.cloud` URL is an example baked into the UPSTREAM storefront template (`storefront/.github/workflows/lint.yml`), not Jolly's specs/assets — nothing for Jolly to clean (it caused a false-positive in my URL sniffing only).
  4. **TRANSCRIPT-KEEPING OMITS THE REAL TRANSCRIPT.** `persistEvalTranscript` saves `agent.stdout/stderr` + traces + `env`, but pi writes its actual reasoning/tool-call transcript to its session JSONL under the throwaway `$HOME` (`.pi/agent/sessions/.../*.jsonl`), which is NOT persisted and is removed in teardown. Observed `agent.stdout.txt` was EMPTY. To "show the pi transcripts" the keeper must persist (scrubbed) the pi session log too.
  5. **EVAL CAN'T EXERCISE THE LIVE STORE STAGE IN BUDGET.** Run outcome (20m01s, scenario FAILED — `@eval` is never a gate): the agent correctly discovered + invoked `jolly start --dry-run`, `auth status`, `auth status --json`, `--help`, `create store --json --yes` (all exit 0), but every real `jolly start --yes` was killed by pi's bash-tool timeout (`Command timed out after 600 seconds`) before provisioning completed; a `429` also appeared. NO environment was created (org `dmytris-organization-1` still has only the pre-existing `jolly-store`). So feature 025's "observe the real live artifacts (dashboard/storefront URL)" assertions can't actually fire here — the live-store affordance is unreachable when one `jolly start` exceeds the agent's per-command budget. Decide: raise/segment the agent budget, make the skill tell the agent to background+poll `jolly start`, or scope the eval to affordance-only and stop asserting live artifacts.
  - **SECRET LEAK — already mitigated this session:** scrubbed all `JOLLY_*`/Stripe values from `/tmp/jolly-eval-transcripts` and removed leftover eval temp roots; advised rotating the Stripe test secret key + Saleor Cloud token (they reached the transcript in plaintext via issue 1).
  - **NEXT:** after the run completes, fresh session → `/qm` to make issue 1 (secret scrub) and 4 (persist pi session log) executable first — those are the concrete, testable ones — then 2/3/5 once dk confirms the intended eval scope. Crew/harness fix, Bosun commit.
- **PUSHED — finish-first hold LIFTED (dk approved 2026-06-17).** `main` pushed to `origin` through `f65dd83`; the per-cycle "NOT pushed — finish-first hold" parentheticals below are historical (state at the time of each cycle), now superseded by this marker.
- **018 PARALLEL-LOGIC LOOPBACK-PORT COLLISION FIXED (2026-06-16, committed locally `7094cea`, NOT pushed — finish-first hold).** A directed `/qm` pass ran `-p logic` and found `018:90` ("login with no flags defaults to the browser URL-first flow") failing intermittently with `TypeError: Invalid URL`. Not an `src/` gap: bare `jolly login` is correct (in isolation it emits a valid `authorizationUrl`; `loginBrowserLive` prints the URL BEFORE binding the port). Root cause is **test isolation** — the two `@logic` live-login scenarios (`018:79` `--browser`, `018:90` no-flags) each bind the fixed, spec-mandated loopback port `127.0.0.1:5375`; under the logic profile's `parallel:2` they can run concurrently, the loser hits `EADDRINUSE`, the CLI honestly returns `OAUTH_CALLBACK_SERVER_FAILED` (no `authorizationUrl`), and the step does `new URL("undefined")`. The port is product behavior and cannot move, so the fix isolates the tests, not the port. **QM-owned harness fix (test-support only — no spec, no `src/`, no scenario text):** a cross-process file-lock mutex (stale-lock backstop) serializing live-loopback scenarios across cucumber workers; teardown now `await`s child exit before releasing so the port is actually freed; `killAndWait` kills immediately instead of waiting out a timeout; the two contended `When` steps get `{ timeout: 60_000 }` since they may wait on the lock. Verified fresh: typecheck clean; `018` under `-p logic` 11/11 x4; full `-p logic` 94 passed / 1 cred-gated skip / 0 failed; `@sandbox` failed-exchange (shares the helper) green; `--dry-run` 0 undefined.
  - **REUSABLE GOTCHA.** A harness server on a FIXED port is shared OS state and cannot run under parallel cucumber workers without a cross-worker lock. Every OTHER harness server binds `listen(0, ...)` (ephemeral OS-assigned port) so only 018's spec-fixed `5375` was exposed. The prior `018` timeout fix (line ~60 below, `120_000` on the failed-exchange `When`) was a different scenario/cause; this is complementary.
- **SANDBOX 429 RESILIENCE — 004:41 STOCK-STAGE RETRY DONE + CENTRALIZED-RETRY INTENT NOW DELIVERED (2026-06-16, committed locally `428cf04`, NOT pushed — finish-first hold).** The follow-up cycle authored in the 005 entry below (the spec-ahead `004:41 @logic @exceptional-double` stock-stage scenario) executed QM→Crew→Bosun and is GREEN. **Crew centralized the retry — the broader Option-A intent, not another per-caller wrap:** `graphqlFetch` (`src/lib/cloud-api.ts`) now retries a transient HTTP 429 at the shared request layer (bounded: `RATE_LIMIT_RETRIES=4`, `RATE_LIMIT_RETRY_DELAY_MS=1000`), so EVERY backend Saleor GraphQL request Jolly's own code sends (stock `seedRecipeStock`, Stripe `queryGetApps`, recipe, app-token, …) retries a momentary rate-limit. **This closes SCOPE GAP (a) from the 005 entry** — `graphqlFetch` itself no longer throws on a single 429. The Stripe stage's existing `withRetries(queryGetApps)` wrappers were intentionally LEFT (they still cover non-429 transient failures on a freshly provisioned env). Verified fresh (not cached): `--dry-run` 0 undefined; typecheck clean; `004:41` 4/4; `-p logic` 94 passed / 1 cred-gated skip / 0 failed (no regression — `005:88` still green).
  - **RESIDUE (honest record).** (b) `Retry-After` honoring is still NOT executable — captured only as prose intent in the 004 Rule "Backend Saleor requests retry a transient rate-limit"; if dk wants it guaranteed it needs its own scenario (stand-in sets `Retry-After: 1`, assert the retry arrived ≥~1s later). (c) `timedGraphql` (the doctor-probe seam) is a separate path NOT covered by `graphqlFetch`'s retry — fine for now (the probes are fail-fast by design, not stage-blocking backend mutations), noted in case a doctor 429-flake ever surfaces.
  - **HARNESS GOTCHA (reusable).** The in-process loopback `@exceptional-double` stand-in had to DISABLE HTTP keep-alive (`Connection: close` + `server.keepAliveTimeout=0`): the bootstrap doctor probe opens a pooled socket to the stand-in early, but the stock stage's request comes only after the multi-second storefront/recipe stages — long enough for an idle keep-alive socket to go stale, so a pooled connection fails the later request with "fetch failed" BEFORE the 429 path runs. Future long-run loopback stand-ins (later stages) must do the same; the 005 Stripe stand-in happened not to hit it.
- **SANDBOX 429 RESILIENCE — 005:88 STRIPE-STAGE RETRY DONE (2026-06-16, committed locally `6573a5e`, NOT pushed — finish-first hold). BROADER CENTRALIZED-RETRY INTENT ONLY PARTIALLY DELIVERED — follow-up needed.** The QM→Crew→Bosun cycle for the spec-ahead `005:88 @logic @exceptional-double` scenario executed and is GREEN. QM made the 4 steps executable via a loopback Saleor GraphQL `@exceptional-double` stand-in (returns 429 once on the Stripe stage's first `GetApps`, then succeeds with the Stripe app already present; `JOLLY_SALEOR_APP_TOKEN` left unset so recipe/stock skip and the single 429 lands on the Stripe stage; `STAND_IN_TOKEN` drives the stand-in). Crew made it pass with the **minimal** change: `installStripeApp` (`src/lib/cloud-api.ts`) now wraps its `queryGetApps` in the existing `withRetries` helper (as `acquireAppToken` does). Verified fresh (not cached): `--dry-run` 0 undefined; typecheck clean; `005:88` green; `-p logic` 93 passed / 1 cred-gated skip / 0 failed (no regression).
  - **SCOPE GAP (Captain, honest record — green but not the full dk Option-A intent).** dk's decision asked to **centralize** transient-429 retry at one canonical request choke point so `graphqlFetch` AND `timedGraphql`/`cloudFetch` all benefit, **honoring `Retry-After`**, blocking only when the 429 persists. What landed is narrower: (a) retry is wrapped at the `queryGetApps` **caller** (`installStripeApp`), so `graphqlFetch` itself still throws on `!response.ok` (429) and its OTHER backend callers — notably the stock stage's `seedRecipeStock` GraphQL — remain unhardened; (b) `withRetries` retries on ANY thrown error with a fixed 5s delay, not 429-specific, and does NOT honor `Retry-After`. The authored scenario only exercises the Stripe-stage seam, so a localized wrap passes it — the spec anchored on `graphqlFetch` to force a centralized fix, but the fix landed one level ABOVE that seam. **The scenario did not force the centralized fix.**
  - **FOLLOW-UP — SPEC-AHEAD DELTA AUTHORED (dk chose "author centralized-retry follow-up", 2026-06-16; uncommitted→committed this turn).** Option (ii) taken (behavior-pinning, not implementation-pinning): a new `@logic @exceptional-double` scenario in `features/004-jolly-configurator-starter-recipe.feature` (under Rule "Recipe products need seeded stock"): **"A transient Saleor rate-limit during the stock stage retries instead of reporting a false blocked"** — points the STOCK stage (`seedRecipeStock`, a DIFFERENT backend caller than `installStripeApp`) at an endpoint that 429s once then succeeds with the recipe in stock; asserts the stock stage is `completed` (retried), NOT `blocked`. A new Rule "Backend Saleor requests retry a transient rate-limit" carries the non-binding design intent: BOUNDED retry (report `blocked`/`fail` honestly when the 429 PERSISTS) that SHOULD honor `Retry-After`. `-p logic --dry-run` surfaces exactly **1 undefined scenario / 4 undefined steps** as the next-QM worklist.
    - **Why stock, not "centralize":** pinning a SECOND backend seam makes the behavior hold wherever it matters; Crew may satisfy it by centralizing at `graphqlFetch`/`cloudFetch` OR by another per-caller `withRetries` wrap — both honest. The behavioral contract (transient 429 → retried, not false-blocked) is what specs enforce; the choke-point refactor stays an implementation preference.
    - **Retry-After honoring: DEFERRED, prose-only intent (NOT yet executable).** Captured in the new Rule as design rationale, not a scenario — a precise `Retry-After` timing assertion is brittle/flaky and post-MVP. **Known green-suite-blindness residue:** prose intent is not enforced; if dk wants `Retry-After` honoring guaranteed, it needs its own executable scenario (e.g. stand-in sets `Retry-After: 1`, assert the retry arrived ≥~1s later) in a later iteration.
    - **NEXT CYCLE (fresh session → `/qm`):** QM makes the 4 stock-stage steps executable (mirror the 005:88 loopback `@exceptional-double` stand-in: key the single 429 on the stock stage's unique first GraphQL query — `queryWarehouseId` — so the preceding recipe-stage configurator spawn does NOT consume it; set `JOLLY_SALEOR_APP_TOKEN` so stock runs, answer the stand-in's warehouse/variants/`productVariantStocksCreate` for success). Crew hardens the stock-stage backend path (or centralizes). Bosun commits; push stays held per finish-first.
  - **ORIGINAL DIAGNOSIS (kept for the trail; the src `blocked` root-cause above is now fixed for the Stripe stage only):** A directed `/qm` pass ran the full sandbox tier and surfaced a roaming, load-induced flake. Diagnosis (confirmed in src): the serial @sandbox tier (~10 min, one shared per-run env, several scenarios doing a SECOND full `jolly start` for idempotency) drives the real Saleor Cloud org into **HTTP 429 rate-limiting** near the suite tail. The 429 lands in different places each run (run1 `005:759`; run2 `004:376`; run3 the Stripe re-run reported the stage `blocked` not `completed`). Root cause of the `blocked`: `runStripeStage` (`src/index.ts:2816-2839`) reports `blocked` whenever `installStripeApp` THROWS, and Jolly's own `graphqlFetch` (`src/lib/cloud-api.ts:418`) throws `CloudApiError` on ANY `!response.ok` including 429 — **no retry** (unlike its sibling `pollTaskStatus`, which already polls/retries the live API). So a momentary rate-limit during the re-run's `queryGetApps` degrades an already-installed stage to a false `blocked`.
  - **QM FIXES (done, verified, in working tree — KEEP):** (a) `features/support/saleor-graphql.ts` — the harness GraphQL client now retries transient 429 (dedicated ~44s exponential backoff, honors `Retry-After`); auth/5xx still throw; this fixed the test-side 429s (`004:376`, `005:759` now pass). (b) `features/step_definitions/018-jolly-auth-commands.steps.ts` — added an explicit `{ timeout: 120_000 }` to the OAuth-failure `When` (`018:595`), which must `await` the real failing token exchange (~75s of internal deadlines) but was inheriting cucumber's 5000ms default and being killed mid-exchange. **This properly fixes the `018:41` flake previously dismissed as "timing variance, untouched" (see the `079f044` entry below, line ~68).** Verified: typecheck clean; `018:41` + `005:79` green in isolation; sandbox runs 2 & 3 cleared both original failures.
  - **dk DECISION (2026-06-16): Option A — harden src + enforce it** (over test-policy-skip or accept-as-flake). Jolly's backend Saleor GraphQL requests must RETRY transient 429s (bounded backoff, honor `Retry-After`) and report `blocked`/`fail` only if the condition PERSISTS past the budget — never degrade an otherwise-successful op to a false `blocked`/`fail` on one momentary rate-limit. Honesty preserved (retry the real call; block only when it genuinely keeps failing). Matches the existing `pollTaskStatus` + harness precedents.
  - **SPEC-AHEAD DELTA AUTHORED (Captain, uncommitted → next `/qm`).** New `@logic @exceptional-double` scenario in `features/005-stripe-checkout-setup.feature` (after the live @sandbox Stripe scenario): **"A transient Saleor rate-limit during the Stripe stage retries instead of reporting a false blocked"** — points the Stripe stage at an endpoint that returns 429 ONCE then succeeds with the Stripe app already installed, asserts the stage is reported `completed` (retried), NOT `blocked`. `@exceptional-double` justified inline (a 429 cannot be produced on demand against the real env; the only double, never the normal path — the real install stays the @sandbox scenario above). `-p logic --dry-run` surfaces exactly **1 undefined scenario / 4 undefined steps** as the worklist. Anchored on the **graphqlFetch seam (the actual blocker)**, not the cheaper doctor-probe seam (which uses `timedGraphql`), to avoid green-suite-blindness — a fix localized to the wrong seam would leave the Stripe stage vulnerable.
  - **NEXT CYCLE (fresh session → `/qm`):** QM makes the 4 steps executable (a loopback Saleor GraphQL endpoint that 429s once then serves a `getApps` result containing a Stripe app; drive the Stripe stage against it; assert `completed`, not `blocked`). **Crew (`src/`):** add bounded transient-429 retry (backoff, honor `Retry-After`) to Jolly's backend Saleor requests — **centralize at one canonical request choke point** so `graphqlFetch` AND `timedGraphql`/`cloudFetch` all benefit (echoes the open "single canonical request choke point" follow-up, line ~124), reporting `blocked`/`fail` only when the 429 persists. Bosun commits the QM fixes + spec + src change. Pushing follows the standing finish-first hold.

- **012 REAL-ENVELOPE URL EMISSION DONE (2026-06-16, committed locally `4fcfd5c`, NOT pushed — finish-first hold still in force).** The QM→Crew→Bosun cycle for the 012 spec-ahead delta (authored in the `7684cc5` entry below) executed and is GREEN against real Saleor Cloud. QM made the two undefined 012 Then steps executable (regex over the real envelope `data`, field-name-agnostic, mirroring the canonical 002 pattern); Crew added `graphqlApiUrl: domainUrl` + `dashboardUrl: new URL("/dashboard/", domainUrl).href` to the REAL create-store success envelope `data` (`src/index.ts` ~1377). **This closes gap item (1)** of the green-suite-blindness list below — the real (non-dry-run) create-store envelope now carries both URLs, so a real `jolly start` store stage emits a dashboard URL. Verified fresh (not cached): `--dry-run` 0 undefined; typecheck clean; the real `@sandbox` 012:91 scenario 17/17 (real env provisioned + torn down); `-p logic` 92 passed / 1 cred-gated skip / 0 failed (no regression).
  - **STILL OUTSTANDING in the finish-eval worklist (the push stays held per dk's finish-first call, line ~53 below):** (2) the deploy stage still doesn't capture/emit the Vercel CLI's printed storefront URL; (3) `eval.ts` `SEEDED_CREDENTIAL_VARS` still seeds `NEXT_PUBLIC_SALEOR_API_URL`+`JOLLY_SALEOR_APP_TOKEN` (so the eval reuses the existing store, never provisions fresh); (4) `eval.ts` still doesn't expose the runner's `vercel login` session under the fake `$HOME`; (5) `HARNESS_EVAL_CLI_TIMEOUT_MS` default still 120s; (6) the feature 025 non-vacuous dashboard-URL Then (authored in `7684cc5`, undefined under `-p eval --dry-run`) is not yet executable. These are the next `/qm` cycle (the 025 half + the eval-harness rewire); the 012 real envelope is now ready to feed them.

- **EVAL URL-EMISSION — QM/CREW CYCLE RAN, PARTIAL (2026-06-16, committed locally `79a321c`, NOT pushed). The discovery-surfaced subset is green; the actual "fix eval" intent is NOT yet delivered.** A `/qm` cycle (QM→Crew→Bosun) worked the `--dry-run` worklist (the 2 undefined feature-002 scenarios / 4 undefined steps) and committed. **What landed & is green:** (a) feature 002 register — `create store --create-environment --dry-run` preview now emits projected `graphqlApiUrl` + `dashboardUrl`, and the two steps assert a `*.saleor.cloud/graphql/` URL + a `.saleor.cloud/dashboard/` URL in `data` (register scenario 15/15); (b) feature 002 deploy — the old combined step was split into a **no-fabricated-URL guardrail** (deployment-status skipped ⇒ no `*.vercel.app` invented) + a `nextSteps` human-gates check; (c) `sandbox.ts` — two stale `SANDBOX_REQUIREMENTS` keys fixed to the current 002 scenario names (`Jolly registers…`, `Jolly connects…`) so the register target runs on the `saleorCloud` token alone; (d) eval plumbing — `HARNESS_EVAL_CLI_TIMEOUT_MS` knob + cucumber step-timeout derived from the agent budget. Verified: `--dry-run` 0 undefined; `-p logic` 92/1-skip; unit 43/43; typecheck clean; 012 12/1-skip (no regression).
  - **GREEN-SUITE BLINDNESS — the eval still cannot deliver URLs (gap, NOT fixed).** QM correctly derives its worklist from `--dry-run` (undefined/failing only), so the rest of dk's "fix eval" intent was invisible to it and remains undone: (1) the **real** (non-dry-run) create-store success envelope (`src/index.ts` ~1367-1378) STILL emits no `graphqlApiUrl`/`dashboardUrl` — only the dry-run PREVIEW got them, but the eval runs the REAL `jolly start`, so it still gets no dashboard URL; (2) the deploy stage STILL never captures/emits the Vercel CLI's printed URL; (3) `eval.ts` `SEEDED_CREDENTIAL_VARS` STILL seeds `NEXT_PUBLIC_SALEOR_API_URL`+`JOLLY_SALEOR_APP_TOKEN`, so the eval still won't provision a fresh store; (4) `eval.ts` STILL doesn't expose the runner's `vercel login` session under the fake `$HOME`; (5) `HARNESS_EVAL_CLI_TIMEOUT_MS` default still 120s; (6) the feature 025 URL-reporting Then (`025:377`) is STILL vacuous (validates URL shape only if one is found; asserts nothing when the store stage yields none).
  - **COMMITTED SPEC/HARNESS MISMATCH (consequence of bundling).** Bosun committed the feature-025 spec-ahead text ("seed ONLY auth creds"; "Vercel session made available to the spawned CLI") together with the partial implementation — but `eval.ts` does neither. `@eval` is never a green/red gate and the changed clauses are prose `Rule:` text (no executable step), so CI won't catch it; main now describes eval behavior that isn't built.
  - **dk DECISION (2026-06-16): FINISH EVAL FIRST, then push.** Nothing reaches `origin/main` half-built; push `79a321c` + the finish together once the eval genuinely returns store/dashboard/deployed URLs.
  - **SPEC-AHEAD DELTAS AUTHORED (Captain, 2026-06-16, uncommitted working tree → next `/qm`).** The gap is now discovery-surfaced (no more green-suite blindness):
    - *feature 012* "Jolly creates a Saleor Cloud environment" (the REAL, namespaced+teardown create path) gained two Then steps — `the envelope \`data\` should report the created store's \`*.saleor.cloud\` GraphQL API URL` and `…Saleor Dashboard URL ending in \`.saleor.cloud/dashboard/\``. Default `--dry-run` now shows **1 undefined scenario / 2 undefined steps** (012:91). These drive Crew to emit the URLs in the REAL create-store success envelope (`src/index.ts` ~1367-1378), not just the dry-run preview.
    - *feature 025* line 129 rewritten so a COMPLETED store stage MUST surface a real `.saleor.cloud/dashboard/` URL (the old `025:377` def passed vacuously). `-p eval --dry-run` now shows **1 undefined step**. Drives the non-vacuous assertion + (via the harness) seed-only-auth + the real emission.
  - **NEXT CYCLE (fresh session → `/qm`):** QM defines the 012 URL Then steps (assert the `*.saleor.cloud/graphql/` + `.saleor.cloud/dashboard/` URLs in the real envelope `data`) and the strengthened 025 Then (store stage completed ⇒ a dashboard URL is mandatory, never fabricated). Harness (QM, `features/support/eval.ts`): drop `NEXT_PUBLIC_SALEOR_API_URL`+`JOLLY_SALEOR_APP_TOKEN` from `SEEDED_CREDENTIAL_VARS` (line ~322) so a fresh store is provisioned; expose the runner's `vercel login` session under the fake `$HOME`; raise the `HARNESS_EVAL_CLI_TIMEOUT_MS` default (still 120s). **Crew (`src/`):** emit `graphqlApiUrl`+`dashboardUrl` in the REAL create-store/start store-stage envelope `data`; capture `npx vercel --prod`'s printed URL → `storefrontUrl`/`deploymentUrl` in deploy `data` (honest absence when gated, never fabricated). Bosun commits; **then push `79a321c` + the finish together** (dk's finish-first call). The deploy-URL is verified live only via 025's eval (no harmless `@sandbox` real-deploy exists); the 002 deploy guardrail covers the no-fabrication side.

- **EVAL URL-EMISSION FIX — SPEC-AHEAD DELTA AUTHORED (2026-06-16, uncommitted working tree → next `/qm`). dk: "fix eval".** Prompted by a live `-p eval` run (dk wanted a real store + dashboard URL FROM the eval): the run passed but surfaced that the eval cannot actually deliver URLs. **Root causes found (all real):** (1) feature 002's two URL assertions are **vacuous step defs** — `002:143` "data should include the new store's `*.saleor.cloud` URL" has an EMPTY body, and `002:394` "data should report the deployed URL" only checks `nextSteps` is an array; (2) **`src` emits no URL at all** — the create-store envelope `data` (`src/index.ts` ~1349-1403) carries `environmentName`/`environmentKey`/`organization` but no API URL (only written to `.env`) and **no dashboard URL anywhere** (grep: "dashboard" in src is only Stripe-config guidance); the deploy stage spawns `npx vercel` but never captures/emits the URL the CLI prints; (3) feature 025 **over-seeds** — it seeded `NEXT_PUBLIC_SALEOR_API_URL`/app-token into the workspace `.env`, so `jolly start`'s store stage saw the store as already configured and **never created one** (the run reused the existing `jolly-store`), directly contradicting 025's own "creates a `jolly-test` store / reports its dashboard URL" intent; (4) the throwaway `$HOME` hid the runner's real `vercel login` session, so the deploy hit the device-auth gate. Also: pi's OWN 300s bash-tool timeout (not a harness knob) killed its monolithic `jolly start --yes` mid-run.
  - **dk DECISIONS (2026-06-16, all 3 recommended):** (a) Jolly emits BOTH the Saleor dashboard URL (derived from the store endpoint) AND the live Vercel storefront URL **captured from the `vercel` CLI's deploy output**; (b) the eval USES the runner's real Vercel session so the deploy actually runs and yields a real storefront URL; (c) the eval CREATES a fresh `jolly-test` store each run (stop seeding the endpoint) — ~3min slot churn, disposable cannon fodder.
  - **SPEC DELTAS AUTHORED (Captain, uncommitted):** *feature 002* — store-creation scenario now requires `data` to include the `*.saleor.cloud` GraphQL API URL AND a Dashboard URL ending `.saleor.cloud/dashboard/`; deploy scenario now requires the deployed storefront URL **captured from the Vercel CLI output, not fabricated/guessed**. *feature 025* — the "live by design" seeding clause now seeds ONLY the auth creds (`JOLLY_SALEOR_CLOUD_TOKEN` + `JOLLY_STRIPE_*`), NOT the endpoint/app-token, so a fresh store is provisioned; the Vercel clause now makes the runner's `vercel login` session available to the spawned CLI (the `$HOME` isolates the agent's own config, but the official CLI sessions the deploy needs are provided). `--dry-run` surfaces 2 undefined scenarios / 4 undefined steps (the 002 URL steps) as the QM worklist.
  - **Next cycle (fresh session → `/qm`):** QM makes the 002 URL steps real (assert the API URL + `.../dashboard/` URL in store-creation `data`; assert the CLI-captured deployed URL in deploy `data`) and strengthens the 025 URL-reporting Then so a COMPLETED store stage MUST yield a dashboard URL (today it passes vacuously). Harness (QM, `features/support/eval.ts`): drop `NEXT_PUBLIC_SALEOR_API_URL`/`JOLLY_SALEOR_APP_TOKEN` from `SEEDED_CREDENTIAL_VARS`; make the runner's Vercel session available to the spawned CLI under the fake `$HOME`; raise the `HARNESS_EVAL_CLI_TIMEOUT_MS` default high enough for a full `jolly start`. **Crew (`src/`):** emit `dashboardUrl` + the API URL in the create-store/`start` store-stage envelope `data`; capture `npx vercel --prod`'s printed URL and emit it (e.g. `storefrontUrl`/`deploymentUrl`) in the deploy `data` — honest absence when gated, never fabricated. Bosun commits once green.
  - **ALREADY IN THE WORKING TREE (uncommitted, operational, from the run):** `features/support/eval.ts` — added the `HARNESS_EVAL_CLI_TIMEOUT_MS` knob (shim's 120s per-call cap killed `jolly start --yes`; default still 120s — QM should raise the default); `features/step_definitions/025-...steps.ts` — derived the cucumber step timeout from the agent budget. These fold into the QM cycle above. Transcript of the passing run: `/tmp/jolly-eval-20260616-112514/`.

- **018 EMPTY-TOKEN HONEST-FAILURE SHIPPED + PUSHED (2026-06-16, `079f044`, origin/main synced @ `53b5752`).** The empty-`--token` reword cycle planned in the entry below executed and is GREEN. New `@logic` scenario **"Jolly login with an empty token fails honestly without blaming the browser"** (018:100) passes. QM made the one undefined Then (`the login error should not claim that browser login is unavailable`) executable as a non-brittle negative invariant — asserts the empty-token failure's `errors`+`summary` text matches no browser-unavailable claim, never exact prose, never naming Playwright. **DEVIATION FROM THE PLAN BELOW (intended, better):** Crew did NOT keep the `NO_LOGIN_METHOD` id and merely reword — it **renamed the code `NO_LOGIN_METHOD` → `EMPTY_TOKEN`** and rewrote the message ("`jolly login --token <value>` requires a non-empty token value") + summary ("No token value was provided. Nothing was written."). `EMPTY_TOKEN` is a valid stable `^[A-Z][A-Z0-9_]*$` id, more honestly names the actual input, and drops the browser-blame + last Playwright residue as a side effect. The branch stays load-bearing for the explicit empty `--token ""` (present-but-empty) path; absent token still routes to the URL-first browser flow. **`NO_LOGIN_METHOD`/"Playwright callback" is now gone from all code, specs, tests, and harness** (grep-confirmed; only these CAPTAIN.md history notes still mention it). Verified fresh (not cached): `--dry-run` 0 undefined; typecheck clean; 018:100 green; full 018 13/14 (the one fail is the `@sandbox` 018:41 real-network loopback OAuth scenario timing out at the 5s cucumber limit — baseline 4.674s, passes ×3 on re-run; timing variance, untouched by this change); `-p logic` 92 passed / 1 cred-gated skip / 0 failed. **dk approved push** (2026-06-16) — `079f044` (fix) + `53b5752` (these notes) on origin/main. No release (the only `src` change is honest error wording on an existing branch, no new runtime capability).

- **018 BARE-LOGIN DEFAULT SHIPPED + PUSHED (2026-06-16, `a56f9a8`, origin/main synced @ `a56f9a8`).** The cycle planned in the entry below executed: the new `@logic` "Jolly login with no flags defaults to the browser URL-first flow" (018:90) is GREEN. QM added the no-flags `When` (mirrors the `--browser` no-browser capture-and-kill step; parameterized `spawnBrowserLogin`/`recordBrowserRun`/`BrowserRun` with an `args` field). **Crew did NOT delete the `NO_LOGIN_METHOD` branch as the prior plan assumed** — it is **load-bearing**: `020:46` (`jolly login --token "" --json`) relies on it to reject an empty/junk token with a stable machine code. Crew's minimal fix: `const browser = args.flags.has("browser") || token === undefined` — bare login (token absent) defaults to browser; an explicit empty `--token ""` (present-but-empty) still falls through to the rejection. Verified fresh (not cached): `--dry-run` 0 undefined; typecheck clean; 018:90 + 020:46 green; full 018 13/13; `-p logic` 91 passed / 1 cred-gated skip / 0 failed. **dk approved push-only** (2026-06-16) — `a56f9a8` on origin/main. **No release** (unchanged from below: URL-first success path still needs the manual human-consent round-trip not in CI).
  - **REVISED GAP + dk DECISION (2026-06-16): the `NO_LOGIN_METHOD` message is now FACTUALLY WRONG; reword it (do NOT delete the branch).** The prior plan to delete the branch + Playwright wording was wrong — the branch is needed (above). What remains is that its message (`src/index.ts` ~332-353) is reached ONLY by empty `--token ""`, yet still says "No token provided" (one was — empty) and "browser login is not available here (no browser/Playwright callback flow)" — which (a) misstates the input, (b) **contradicts binding rule "a missing browser is never an error"** (018 rules 139/186) by blaming the browser, and (c) carries the last Playwright reference in the repo. `020:46` pins only the *contract* (status≠success, stable `^[A-Z][A-Z0-9_]*$` code), not the prose, so the wording is free to fix. **dk chose "spec it + fix (cycle)"** over a copy-only patch or backlog.
  - **SPEC-AHEAD DELTA AUTHORED (uncommitted working tree — survives a context clear → next `/qm`).** New `@logic` scenario in `features/018-jolly-auth-commands.feature` **"Jolly login with an empty token fails honestly without blaming the browser"**: `When jolly login --token "" --json` → reuses the 020 stable-code Then + the no-`.env`-write Then, plus ONE new Then **"the login error should not claim that browser login is unavailable"** (negative invariant tied to binding rule 139/186, non-brittle — asserts the empty-token failure doesn't blame the browser, not exact prose). NB: the step deliberately does NOT name Playwright — asserting "must not mention Playwright" would itself reintroduce the reference into the spec corpus (dk's correction). Rewording the message to stop blaming the browser removes the Playwright parenthetical as a side effect. `--dry-run` confirms exactly **1 undefined scenario / 1 undefined step** (that new Then).
  - **Next cycle (fresh session → `/qm`):** QM makes the new Then executable (assert the error message/presentation text makes no browser-unavailable claim — e.g. matches none of /browser.*(unavailable|not available)|no browser/i). **Crew** rewords the `NO_LOGIN_METHOD` message so it honestly describes an empty/missing token value WITHOUT claiming the browser is unavailable and WITHOUT naming Playwright (branch stays; code `NO_LOGIN_METHOD` may stay — it's a valid stable id). Bosun commits once green; then re-assess release.

- **018 REAL (NON-DRY-RUN) BROWSER-LOGIN FIX SHIPPED + PUSHED (2026-06-16, `12fbc64`, origin/main synced @ `12fbc64`).** The QM→Crew→Bosun cycle planned in the `951edb6`/`8bca3be` entry below executed in full. Both spec-ahead scenarios are GREEN: the new `@logic` "Jolly login --browser never treats a missing browser as an error" (real non-dry-run path presents the URL + reports the loopback callback endpoint + never errors on a missing browser) and the re-aimed `@sandbox` "A failed OAuth code exchange is reported honestly" (real loopback callback delivers a Keycloak-rejectable code → real failing POST to the auth.saleor.io token endpoint → honest error envelope). Crew added `loginBrowserLive` (real loopback server on `127.0.0.1:5375` + real PKCE exchange), replacing the old `BROWSER_LOGIN_UNAVAILABLE` `--browser` branch. QM repurposed the three orphaned old failed-exchange step defs in place + added a live loopback driver. Verified fresh (not cached): `--dry-run` 0 undefined; typecheck clean; both targets green; full 018 12/12; `-p logic` 90 passed / 1 cred-gated skip / 0 failed. **dk approved push-only** (2026-06-16) — `12fbc64` on origin/main. **No release** (dk's call): the URL-first success path still needs a manual human-consent round-trip not exercised in CI, so the new runtime capability isn't release-gated yet. A version bump + npm publish remains a separate dk decision.
  - **FOLLOW-UP GAP + dk DIRECTIVE (2026-06-16): purge the stale `NO_LOGIN_METHOD`/Playwright residue; bare `jolly login` = de-facto URL-first.** Bosun flagged, and dk confirmed, that bare `jolly login` (no `--token`, no `--browser`) at `src/index.ts:328-349` still returns a `NO_LOGIN_METHOD` error whose message names a "no browser/Playwright callback flow" — the **only** remaining reference to the purged Playwright/login-method framing anywhere in the repo (grep-confirmed: nothing in specs, AGENTS.md, or harness; CAPTAIN.md notes excepted). It lingered because the two prior cycles only touched `--dry-run` and the `--browser` branch, and no scenario covers bare `jolly login` (green-suite blindness, lesson #1). dk: "no reference to that left anywhere, only the de-facto." **Decision (dk, 2026-06-16): Spec it now.** I authored a new `@logic` scenario **"Jolly login with no flags defaults to the browser URL-first flow"** — bare `jolly login` where no browser can be opened must present the authorization URL, not present a missing browser as an error, direct the user to open the URL or use `--token`, write no token. It deliberately carries **no** Playwright/login-method wording itself; the positive de-facto assertions (presents URL + non-error) alone make the stale branch a red target. Steps maximally reused — only the `When` is undefined.
  - **Next cycle (fresh session → `/qm`):** `--dry-run` surfaces exactly **1 undefined scenario / 1 undefined step** (the bare-login `When`; same capture-and-kill pattern as the `--browser` no-browser scenario). QM makes it executable; **Crew** routes bare `jolly login` (no flags) to `loginBrowserLive` and **deletes the `NO_LOGIN_METHOD` branch + the "Playwright callback flow" wording entirely** (`src/index.ts:328-349`). Bosun commits once green; then re-assess the release decision (the new runtime capability now covers bare login too).

- **018 URL-FIRST DRY-RUN INCREMENT SHIPPED + PUSHED (2026-06-16, `951edb6` + `8bca3be`, origin/main synced @ `8bca3be`).** The QM cycle for Decision 1 (below) executed: feature 018's new `@logic` "presents the authorization URL and offers to open a browser" scenario is GREEN (4 Then steps made executable; the human-facing surface — summary + nextSteps — now states URL-first behavior), and the orphaned `@requires-browser`/Playwright harness was removed (`features/support/browser.ts` whole file, the `@requires-browser` Before hook + imports in `hooks.ts`, the `SANDBOX_REQUIREMENTS` entry in `sandbox.ts`, the stale Tier-2 `loginInput` field in `world.ts`). Verified: `--dry-run` 0 undefined; `-p logic` 89 passed / 1 cred-gated skip; typecheck clean. **dk approved the push** (2026-06-16); both commits are on origin/main. No release/version bump — the only `src` change was the `login --browser --dry-run` output wording, no new runtime capability.
  - **GAP FOUND + dk DECISION (2026-06-16): FIX NOW the real (non-dry-run) login path.** The committed work only covers `--dry-run`. The REAL `jolly login` / `jolly login --browser` (`src/index.ts:318-359`) still returns an error envelope (`BROWSER_LOGIN_UNAVAILABLE` / `NO_LOGIN_METHOD`) on a headless box, directing to `--token` — **honest, but it contradicts the now-binding rule "a missing browser is never an error"** (018 rules 116-118, 162-166). No automated scenario caught it (the only real-path scenario, the `@sandbox` failed-exchange, *tolerated* the error) — classic green-suite blindness (lesson #1). dk chose **Fix now** (over defer/leave-as-is).
  - **SPEC-AHEAD DELTA AUTHORED (uncommitted working tree — survives a context clear → next `/qm`).** Two scenario changes in `features/018-jolly-auth-commands.feature`:
    1. **New `@logic`** "Jolly login --browser never treats a missing browser as an error" — real (non-dry-run) `jolly login --browser` where no browser can be opened must: present the Keycloak authorization URL (click/copy-paste), report the loopback callback endpoint `http://127.0.0.1:5375/callback` (started, not just the dry-run redirect_uri), NOT present a missing browser as an error, direct the user to open the URL or use `--token`, and write no token. (Non-interactive runs must return rather than block — the human consent round-trip stays manual, not CI.)
    2. **Re-aimed `@sandbox`** "A failed OAuth code exchange is reported honestly" — was satisfied by the `BROWSER_LOGIN_UNAVAILABLE` cheat, which the fix removes; now drives a REAL exchange failure by running `jolly login --browser` and **delivering a Keycloak-rejectable authorization code to the loopback callback** (simulates only the browser *redirect*, not the human consent), asserting the real POST to the auth.saleor.io token endpoint really fails with an honest error envelope, nothing written. Honors rule 165 ("exchange scenarios" are covered in CI).
  - **Next cycle (fresh session → `/qm`):** `--dry-run` surfaces **2 undefined scenarios / 7 undefined steps** as the worklist. QM makes them executable (the `@logic` one needs forced no-browser + non-interactive/bounded-return; the `@sandbox` one needs a concurrent-callback harness: run the CLI awaiting the callback + deliver the bogus code to `127.0.0.1:5375/callback`). **Crew** then implements the real callback-server login path in `src/` so the headless real path presents the URL + starts the server + never errors for a missing browser, and the bogus-code exchange really fails honestly. Likely orphaned step defs from the old `@sandbox` wording ("Jolly receives an authorization code that Keycloak will reject", "it exchanges the code with the Keycloak token endpoint", "the exchange request should really be sent and really fail") → Bosun prunes. If the concurrent-callback harness proves a genuine blocker, QM loads Captain. Commit via Bosun once green; that commit (real `src` browser-login capability) likely warrants a release decision then.

- **LIVE-BY-DESIGN HARNESS REWRITE DONE + COMMITTED LOCALLY (2026-06-16, `3c7d1f5`, NOT pushed).** The directed `/qm` live-by-design cycle (planned in the entry below) executed in full and committed via Bosun once 026 went green. The forced-safe `logicSafeEnv`/`DUMMY`/`.invalid` harness is gone; the verification layer runs against real `.env` services (real absence / real bad input / real CLI-state probes), and feature **026 "no forbidden double" conformance is GREEN**.
  - **Done:** all ~131 normal-path doubles flipped to real across ~20 step files; `logic-env.ts` + the 3 `*-cli-fake.ts` modules deleted (real Stripe-CLI trace wrapper salvaged to `stripe-cli-trace.ts`); new `features/support/creds-env.ts` (`absentCredentialsEnv` unsets the runtime creds = real absence; `STAND_IN_TOKEN` drives the local loopback Cloud-API/GraphQL fixtures, which 026 does not flag). The two `@exceptional-double` carve-outs (012 env-limit via local limit-harness; 018 Cloud-API-unreachable via an inline-annotated `.invalid` host) are now recognized by the 026 scanner. **001:20** re-aimed to exclude the honest `vercel-auth` capability check (a real `vercel whoami` pass is a real result, not fabrication). The absorbed vercel-auth WIP (014/020/`sandbox.ts`/`src/index.ts`/`src/lib/hosts.ts`) landed in the same commit. The `@eval` harness (025/`eval.ts`) was rewritten to seed real test-env creds + `jolly-test` namespacing/best-effort teardown + the live-URL/`HARNESS_EVAL_KEEP_STORE` retention steps.
  - **005:71 flake — resolved differently than hypothesized.** Root cause confirmed: with the fakes gone, a real `jolly start --yes` genuinely does the storefront stage's `git clone` + `pnpm install` (~90s) — credential-independent, so it really completes — which timed out under `parallel=4`. dk-approved resolution: raise those scenarios' `runCli` timeouts (90s→240s) + lower logic `parallel` 4→2 (cucumber.js); 002's storefront no-fabrication assertion re-aimed to verify a *real* clone (`storefront/package.json`) rather than asserting it never completes. The "lighter/gated doctor vercel spawn" idea was **not needed** — left as a non-blocking future optimization.
  - **Verified:** typecheck clean; default `--dry-run` 0 undefined; `-p logic` 88/88 (1 skipped, 0 failed); 026 green; `-p eval --dry-run` 0 undefined. The live `@eval` run was NOT executed here (needs the `pi` runner + drives real cloud resources; gated/slow) — its harness is structurally migrated and dry-run-clean only.
  - **Deck:** clean; local commit `3c7d1f5` on `main`, **NOT pushed**. No version bump/release (harness + the small absorbed src vercel-auth check). **Outbound pending dk approval:** push `3c7d1f5` to `origin/main` (trunk-based). A release/republish is a separate dk call — `src` behavior change here is only the `vercel-auth` doctor check.

- **026 KEYSTONE NOW EXECUTABLE + RED — green-suite-blindness gap CLOSED (2026-06-15, QM cycle, UNCOMMITTED → executed; see entry above).** QM drove the 4 undefined `--dry-run` targets to executable:
  - **020** first-party re-aim: GREEN (Crew dropped `api.stripe.com` from `src/lib/hosts.ts`; step asserts neither Vercel nor Stripe host in Jolly's own request code).
  - **014** vercel-auth ×2: GREEN (Crew added the `vercel-auth` doctor check spawning `vercel whoami`; `@logic` "not logged in" skips live when the runner has a session — honest live-by-design skip; `@sandbox` "logged in" wired into `SANDBOX_REQUIREMENTS`+`VERCEL_CLI_SCENARIOS`).
  - **026** conformance `@property`: now executable and **RED by design** — `features/step_definitions/026-live-by-design-verification.steps.ts` scans step-defs + support and enumerates **131 forbidden doubles** (119 dummy/forced-safe-credential = `logicSafeEnv`/`DUMMY` across 22 files; 8 fake-cli = 3 modules + importers in 004/005/025/eval; 4 unroutable `.invalid`), exempting only sites annotated `@exceptional-double` naming the condition. This is exactly the "discoverable red target" lessons #1/#2 wanted: the no-fakes sweep is no longer invisible prose. typecheck clean; `--dry-run` 0 undefined.
  - **Scope/classification was ALREADY decided — no new Captain decision needed.** The GOVERNING DESIGN DECISION + Failure-condition production policy (below) fully determine it: **make-real everything**, with exactly **two `@exceptional-double` carve-outs** (`ENVIRONMENT_LIMIT_REACHED` 012:108, unverifiable-endpoint "stored, not verified" 018:6 — both already tagged). The re-aim's per-double work is execution against a settled policy, not discovery. NB: the two tagged exception SITES still reuse `logicSafeEnv`/`.invalid` without an inline `@exceptional-double` annotation, so 026 currently flags them too — annotating those two sites (naming the condition) is part of making 026 green.
  - **Deck:** UNCOMMITTED. **Bosun verification recheck (logic profile) found the increment is NOT clean — vercel-auth entangled it with the live-by-design re-aim, as predicted.** Beyond the sanctioned 026 red:
    - **001:20 deterministic regression** — `doctor-vercel-auth must not be a fabricated pass` (`001-…steps.ts:308`). src is HONEST: `vercel whoami` genuinely succeeds (runner has a real `vercel login` session), so `vercel-auth`=`pass`. The integrity scenario's over-broad regex `/deploy|storefront-deployed|vercel/i` wrongly nets the honest auth-CAPABILITY check. **Fix = QM step-assertion re-aim** (narrow to deploy/stage COMPLETION ids — `vercel-deployed`/`storefront-deployed`/`deployment-status` — excluding the `vercel-auth` capability check). This is the "re-aim the assertion at its real intent" policy above, forced early by 014. NOT a src/spec defect.
    - **005:71 intermittent parallel flake** — passes serial/isolated, failed 2 of 3 parallel logic runs; correlated with the new live `npx vercel whoami` spawn now firing in every `jolly start` auto-doctor across parallel workers. Needs diagnosis (likely same assertion class; possibly a design call on whether doctor's vercel-auth spawn should be lighter/gated rather than unconditional `npx vercel` per run).
    - **Implication:** 014 cannot land clean in isolation; the 001:20/005:71 assertion re-aim is the LEADING EDGE of the directed re-aim cycle, pulled forward.
  - **DECISION (dk, 2026-06-15): ABSORB INTO THE RE-AIM CYCLE — nothing commits now.** The whole increment (020/014/026 step-defs + `sandbox.ts` + `src/index.ts` + `src/lib/hosts.ts`) stays UNCOMMITTED on disk and is carried into the next directed `/qm` live-by-design cycle; a context clear doesn't touch the working tree, so it survives the fresh session.
  - **Next cycle (fresh session → `/qm`), driven by a firewall-safe human-scoped directive (lesson #6 channel — QM can't read CAPTAIN.md, but the worklist already lives in QM-readable surfaces: the 026 red output lists every double with `file:line`, and AGENTS.md carries the policy):** make 026 green by flipping the 131 normal-path doubles to real `.env` services (namespace/teardown, harmless-by-design) and annotating the two `@exceptional-double` carve-out sites; re-aim 001:20/005:71 to target deploy/stage COMPLETION ids (exclude the honest `vercel-auth` capability check); diagnose the 005:71 flake (consider a lighter/gated doctor vercel spawn vs unconditional `npx vercel whoami`). Execution shape (lesson #3): centralize the env seam first (behavior-preserving, green-guarded), then flip area by area and dispose the fakes. Then the `@eval` live-URL/retention harness work. Commit via Bosun once 026 is green.
- **LIVE-BY-DESIGN SPEC SCAFFOLDING LANDED + PUSHED (2026-06-15, `3cd5ddf`, origin/main synced).** Spec-only checkpoint, driven entirely by good specs (no ad-hoc QM direction): feature **026** conformance `@property` "no forbidden double" (the de-fake keystone — makes the no-fakes rule a discoverable red target); `@exceptional-double` tags on the two admissible doubles (**012** env-limit, **018** Cloud-API-unreachable); doctor **014** `vercel-auth` check via `vercel whoami` (logic absence + sandbox presence) + single-oracle delegation principle; **020** Stripe-mirrors-Vercel (dropped `api.stripe.com` from Jolly's request allowlist — vestigial; Stripe reached only via the spawned CLI); rule refinement (AGENTS.md/SCENARIO_WRITING.md/CAPTAIN.md — *testability, not subject*, admits conformance invariants). QM worklist was the 4 undefined `--dry-run` targets (026 conformance, 014 vercel-auth ×2, 020 first-party) — **all now executed; see the top entry.** No release at the time: `src` unchanged, suite intentionally spec-ahead. The delegation principle largely *codified* existing design (005 already imports Stripe keys read-only via the CLI; Vercel already CLI-only).
- **RECONCILED STATE (2026-06-15, post-QM verify):** deck clean; `main` == `origin/main` @ `28c3c96` — 3 commits past the 0.6.2 baseline below (`67b9cfe` spec re-aim, `e6beb26` 024/005 real @sandbox targets + orphaned-fake prune, `28c3c96` eval live-URL/retention specs). Verify: typecheck clean; default `--dry-run` 0 undefined; `-p logic` 87/87; `-p sandbox` 30 passed / 5 cred-gated skips / 0 failed.
  - **Live-by-design cycle: PARTIALLY landed, NOT complete.** Done: spec re-aim + the 2 surfaced @sandbox targets (024/005) + eval spec deltas. **Still open — green suite does NOT prove this done:** (1) the AGENTS.md conformance sweep — `logicSafeEnv`/`.invalid`/`DUMMY` still referenced in ~25 step files; `logic-env.ts`/`dotenv.ts`/`stripe-cli-fake.ts`/`configurator-cli-fake.ts`/`storefront-cli-fake.ts`/`eval.ts` still on disk; (2) the @eval harness rewrite — `-p eval --dry-run` = 1 scenario / 5 undefined steps (the live-URL + retention `Then`s). `@iteration` 43 undefined = deferred backlog, not a gate.
  - **QM-discovery gap to flag:** the conformance sweep is intent (AGENTS.md) not enforced by any red test, so QM's verification-driven worklist won't surface it on its own — only the 5 `@eval` undefined steps show as a real target. Drive the sweep by explicit direction (or by encoding it as failing checks).
- **CONSISTENT BASELINE for the next iteration (2026-06-15, updated post-0.6.2):** all surfaces synced before the live-by-design harness rewire.
  - **git:** `origin/main` @ `baf5920`, synced; deck clean; tag `v0.6.2`.
  - **npm:** `@dk/jolly@0.6.2` published (registry confirms `0.6.2`). 0.6.2 ships a **real CLI behavior change** (unlike 0.6.1's test-tier-only deltas): `jolly doctor saleor`'s `saleor-endpoint` check now runs a real read-only GraphQL connectivity probe (`probeEndpointConnectivity`, cloud-api.ts) → `pass` reachable / `unknown` present-but-unreachable / `fail` absent. Carries forward the 0.6.1 src (v1 worklist `feb52f8` + `NON_FIRST_PARTY_HOST` guard).
  - **homepage:** Vercel production deploy READY; `jolly.cool/setup` live (HTTP 200, current `assets/homepage/setup.md`).
  - **verified at release:** typecheck clean; `--dry-run` 0 undefined; `-p logic` 94/94 green; `-p sandbox` 30 passed / 4 cred-gated skips / 0 failed.
  - **QM cycle just closed (commits `4eb1548` + release `baf5920`):** fixed 3 failing @sandbox targets — doctor connectivity probe (002, the end-state behavior); 012 infer-org now resolves org against **real** creds; 022 idempotency-outline assertion de-hardcoded. **Stepping-stone caveat:** the 012 fix *kept* `logicSafeEnv` on the @logic normalize path — the live-by-design iteration below still owns ripping that out.
  - **Intended pending state (NOT a regression):** the `@eval` spec is ahead of its harness (4 undefined steps) — that is the QM worklist for the next iteration, by design.
- **Captain spec re-aim DONE (2026-06-15) — handover baseline for the live-by-design QM cycle.** Survey finding: the spec corpus was already mostly live-by-design-correct; the violation is overwhelmingly in the HARNESS, not the spec text. The "does not fabricate…" integrity scenarios (004/005/014/020) already feed real absence ("no real credentials" / "cannot reach a real store"), so they need NO spec change — only a step rewrite to real. So the spec re-aim is deliberately small (per dk's hybrid call: rebuild launch-bar branches as real @sandbox, drop the rest):
  - **024:** dropped the 4 in-process-GraphQL-stand-in `@logic` branch scenarios (refuse-SMTP / create-when-absent / reuse-idempotent / refuse-low-perm); folded their safety intent (dedicated "Jolly Setup" app, never a stranger; idempotent no-duplicate reuse) into the real @sandbox S6.
  - **005:** re-tagged the Stripe-CLI-import `@logic`→`@sandbox` (real `stripe` CLI, capability-gated skip-not-fail); dropped the 2 CLI-session edge scenarios (explicit-flags-override, doctor-recognizes-CLI-keys) as YAGNI. Their behavior still exists in src and is still documented in the 005 Rule prose (untested-but-present — prune later if desired).
  - **Surfaces to QM via verification:** 2 undefined @sandbox targets (`024:21`, `005:41`) to rebuild real; the 024 in-process GraphQL stand-in (`startInstanceGraphql`) + `stripe-cli-fake` step-defs are now ORPHANED → remove.
- **QM worklist for the live-by-design cycle (= the 2 surfaced targets + the standing AGENTS.md conformance sweep).** AGENTS.md already forbids fakes (QM-visible) but a green suite won't point at the rest, so QM must treat AGENTS.md conformance as in-scope, not wait for a red test: rip `logicSafeEnv`/`.invalid`/`DUMMY` from the ~19 step files (incl. the 012 normalize-path stepping-stone above), delete `stripe-cli-fake.ts` + `configurator-cli-fake.ts` + `storefront-cli-fake.ts` + the 024 GraphQL stand-in, rewire all tiers to real `.env` (namespace/teardown/cannon-fodder reclaim), apply the failure-production policy, and make the 4 undefined `@eval` steps executable. Justified-double carve-outs stay: `ENVIRONMENT_LIMIT_REACHED` and the unverifiable-endpoint "stored, not verified" path (e.g. 005 doctor checkout-readiness with no reachable store). Needs a fresh session → `/qm`.
- **Eval re-aim addendum (dk, 2026-06-15): live eval must hand back usable URLs.** dk asked for "fresh storefront + dashboard URL" from eval; root cause is the known spec-ahead-of-harness gap (`eval.ts` still forced-safe: `logicSafeEnv` workspace `.env` + `.invalid` base + `writeFakeStripeCli`). Two small spec deltas added to feature 025 on top of the existing live re-aim:
  - **Report live URLs:** new scenario `Then` — for each live stage completed, surface the real endpoint Jolly emitted (Saleor dashboard URL for the created `jolly-test` env; storefront URL when the Vercel deploy completed; honest absence when gated). Plus a Rule clause.
  - **Opt-in retention:** `HARNESS_EVAL_KEEP_STORE` (set → retain) skips teardown so the run's `jolly-test`-namespaced store + Vercel deploy stay standing and the URLs stay usable; **default unset → normal best-effort teardown** (harmless-by-design preserved). Retained resources keep the `jolly-test` namespace, so the next run's leftover-reclamation removes them. Operability-only, never gates pass/fail (mirrors `HARNESS_EVAL_TRANSCRIPT_DIR`). New `HARNESS_*` knob — AGENTS.md harness-knob list wants it added (tooling-config, not a Captain edit).
  - **Adds to the QM @eval worklist:** the new URL `Then` + the reworded teardown `Then` are undefined until the live harness rewrite lands; folds into the existing "make the @eval steps executable / rip out forced-safe `eval.ts`" item. To actually produce URLs now: after the live harness lands, run `-p eval` with `HARNESS_EVAL_KEEP_STORE` set + `HARNESS_EVAL_TRANSCRIPT_DIR` to capture them.
- **Remaining to v1 ship:** one real paste→live-store acceptance pass to the feature 002 launch bar — deployed URL works, browse/cart against Saleor Cloud, checkout reaches the Stripe test payment step, `jolly doctor` checkout probe passes. Prefer a fresh blank Saleor environment (a non-blank store makes configurator deploy block honestly instead of applying). The `002` deployed-storefront `@sandbox` scenario is defined and skip-not-fail locally until a live deploy exists to point at.
- **Open follow-up (architectural, non-blocking):** the `NON_FIRST_PARTY_HOST` guard sits only at the `graphqlFetch` seam (where the customer `--url` flows). Sibling seams (`cloudFetch`, `pollTaskStatus`, `timedGraphql`) take only internally-derived first-party URLs and are unguarded — no scenario exercises them. If a future scenario lets a customer-supplied host reach them, centralize the predicate at one canonical request choke point rather than spreading `assertFirstPartyUrl`.
- **Deferred past v1:** tracked by the `@iteration` tag in the specs (`cucumber-js -p iteration`); the v1 worklist (002/008/009/012/020) is satisfied. Notable backlog items: `009` multi-agent detection matrix, `006` command-surface/flag-matrix robustness, `007` failed-skill-install surfacing, `017` auto-apply safe skill update, `002` resume-skip, `003` Paper guidance-in-plan.

## Lessons learned → Shipshape upstream (2026-06-15)

Process lessons from the live-by-design design session. Workflow-generic (not Jolly-specific) — candidates for upstreaming to Shipshape's role prompts / scenario rubric.

1. **Green-suite blindness.** QM derives its worklist only from undefined/failing targets, so a suite that is green *with* forbidden fakes yields an empty worklist — the violation is invisible. A clean `/qm` run reported "nothing to do" while ~140 fake sites sat in the harness. *Upstream:* a methodology rule that is not encoded as a failing check is unenforced; the workflow must let conformance be a discoverable target, not only prose in the agent-config doc.

2. **Testability, not subject, is the line for what's specifiable.** We (and the "no @meta/harness feature" guidance) over-read methodology as unspecifiable. The real discriminator is falsifiability: untestable aspiration is banned; a *testable* harness/conformance invariant ("no forbidden double outside tagged sites") is a legitimate `@property` scenario — same family as the existing no-fabrication / first-party-host invariants. *Upstream:* state it as "testable ⇒ specifiable, regardless of subject"; admit conformance invariants as first-class scenarios so methodology self-enforces.

3. **Disposability has a granularity prerequisite.** "Specs durable, code disposable" yields clean regeneration only when the disposable unit is modular enough to match what you want to replace. A concern smeared across 19 files (shared fake-env import, 113/123 scenarios) makes blunt delete-and-regen "too much" — it nukes correct coverage too. Fix: refactor-to-seam first (behavior-preserving, green-guarded), then dispose/flip surgically. *Upstream:* the regen model needs a modularity precondition, and a behavior-preserving refactor is a distinct cycle shape — green-guarded, human-directed, **not** red-discovered — that the red→green QM loop doesn't natively cover.

4. **The disposable layer is three, not two.** "Code disposable" reads as `src/`, but the fakes lived in the *test harness* (step defs + support) — neither spec nor implementation. Deleting `src/` regenerates against the surviving faked harness, so fakes persist. *Upstream:* name the verification/harness layer explicitly as its own disposable-from-specs layer with its own conformance, distinct from product implementation.

5. **Encode constraints as positive observables, not prohibitions.** "Jolly shall not call the API directly" as prose is an unenforced rule. The enforceable form is a positive scenario whose observable is the desired behavior ("Jolly invokes the official CLI") via the rubric's "the request is the contract" exception. *Upstream:* guide authors to express security/architecture constraints as positive falsifiable observables, never "shall not" prose.

6. **Directed work needs a firewall-safe channel.** QM's context firewall rightly rejects chat/prose worklists — but then genuinely non-red-discoverable work (refactors, conformance seeding) has no entry except an explicit human-scoped instruction in the fresh session. *Upstream:* define a sanctioned "directed cycle" handoff — a durable, QM-readable scope artifact — so directed work is possible without leaking Captain discovery context.

7. **On a role takeover, the predecessor's flagged blockers are the primary input — the role prompts don't enforce it.** (2026-06-17, observed live.) Captain took over after a QM→Crew→Bosun cycle whose final report explicitly listed three blockers/open-questions for Captain (scenario-5 namespacing fork, scenario-8 weak coverage, parallelism flakiness). Captain instead led with the post-Bosun outbound decision and then imported a large, partly-stale `CAPTAIN.md` ad-hoc worklist (configurator flags, deploy failure, harness export) as if it were the live agenda — dropping the actual handoff until dk forced "tell me verbatim the blockers QM left." Two prompt defects caused it: (a) the Captain opening checklist frames the takeover as ONE bucket to pick (*"identify whether this is discovery, spec maintenance, blocker resolution, dirty-deck routing, or post-Bosun outbound"*), inviting tunnel-vision when a real handoff is several at once; (b) the post-Bosun path says "summarize the work and offer outbound next steps" with no instruction to first consume the outgoing role's final-report blockers, and nothing prioritizes the fresh handoff over accumulated historical notes. *Upstream:* every role prompt's opening should make the immediately-preceding role's final-report blockers/open-questions the FIRST agenda item, read verbatim, addressed before discovery/spec/outbound; state that a takeover is often several situations at once (handle all, blockers first); and rank the fresh handoff above accumulated notes on conflict. (Local stopgap added to this repo's `AGENTS.md` → "Role handoffs" under Shipshape Workflow.)

## File-placement principle

- `features/*.feature` + `assets/**` = product intent (binding). `CAPTAIN.md` = non-binding notes. `AGENTS.md` = Shipshape/tooling-generic agent config (no product specifics, bar unavoidable tooling identifiers like `@dk/jolly`, `JOLLY_*`). `CLAUDE.md` = thin Claude-Code-specific pointer to `AGENTS.md`.
- All test/harness methodology lives in `AGENTS.md` so QM/Crew always see it — the tiers, *real services always (never mock/fake)*, *harmless-by-design*, the sandbox provisioning/teardown mechanics, and *specs map to executable steps, no design notes as steps*. `features/**` is the home of product intent, not a place to narrate the harness. **Refined (2026-06-15):** the one admitted exception is a *testable conformance invariant* about the verification layer (feature 026's `@property` "no forbidden double"). The discriminator for what may be a scenario is **testability, not subject** — an untestable aspiration is barred whatever its subject, while a falsifiable invariant is admissible even when its subject is the harness. This is what makes *real-services-always* self-enforcing instead of prose-only (see Lessons learned #1, #2). The earlier "no harness/@meta feature" wording over-applied: it was meant to bar untestable aspiration, not testable harness invariants.

## Goals & MVP framing (the real objective)

- **North star:** an agent takes a customer from a homepage prompt to a real, live, honest storefront — and the customer's own agent owns it afterward. Success is defined by the launch bar below, not by feature count.
- **MVP first, then iterate:** ship one clean, honest end-to-end path before breadth. Don't over-engineer or chase edge cases the first run won't hit — get paste→live-store working, then iterate. Setup is v1; the iteration phase (feature 019) comes after.
- **Honesty is non-negotiable:** never fabricate success. "Verified", `pass` checks, and success claims only for work actually performed and confirmed; unverified work says so; unimplemented paths error honestly. This is the trust the whole product rests on (enforced as behavior in features 018/020).
- **Empower, don't replace, the agent:** Jolly does deterministic plumbing and orchestrates the official CLIs; the customer's agent stays in charge — approves risk, completes human gates, owns the store after setup.
- **Audience:** AI agents/skills are the primary consumers; human DX stays decent but secondary.

## Product identity (non-binding)

- Name: Jolly. Tagline: "Ahoy, agent. Go build a store."
- A tool by Dmytri Kleiner to help an agent set up a Saleor + Vercel + Stripe store fast. Not an official product of Saleor, Vercel, or Stripe.
- Product shape: homepage + thin CLI + Jolly skill + Saleor agent-skills.
- **Launch bar** (when v1 is good enough to ship): homepage prompt → real deployed storefront → browsing/cart against Saleor Cloud → checkout reaches the Stripe test payment step, every claim verified, nothing fabricated.

## Architecture framing (non-binding; behavior is specified in features 002/008)

- `jolly start` is agent-supervised orchestration: it runs the mechanical stages by spawning the official CLIs and Jolly helpers, pauses for approval/risk context, and waits at human gates. The ordered stages and their contracts are specified in features 002 and 008 — not enumerated here.
- Human-run `jolly start` is the backup path for interactive gates a non-TTY agent cannot complete. The homepage stays paste-to-agent first.

## Open questions (deferred out of features)

- `features/001`: whether Jolly should create project-local durable artifacts such as `.jolly/` reports or state; exact per-environment setup steps for supported agent targets.
- `features/008`: whether `jolly create app-token` should request all available permissions or allow the agent to specify a subset.
- `features/012`: which pasted URL forms Jolly should normalize in v1; exact Saleor API or Dashboard automation path for app-token creation; exact task-status response shape and domain extraction against the live Cloud API.
- `features/019`: mcp-graphql config format for each supported agent environment; whether `jolly doctor --watch` belongs in v1 or v2; upgrade cadence signal for outdated skills.
- `features/020`: envelope schema versioning; canonical registry of stable error codes and check ids.
- `features/021`: whether `riskLevel` is derived deterministically from categories or set per action; optional fields such as estimated cost or affected record counts.
- `features/022`: how Jolly detects completed remote work, and whether resumable state is tracked in local artifacts such as `.jolly/`.
- `features/023`: CI wiring for sandbox credentials.

## Non-binding notes

- The product stack is specified in features, not restated here: Paper storefront + Vercel (002), Saleor Cloud only / no self-hosted (003), the configurator starter recipe (004), Stripe test mode only (005).
- Post-setup customization belongs to the customer's own agent/workflow.
- Homepage/setup-guide copy principle: less is more; avoid junk and duplication.
- Open future item: the npm tarball is large because `assets/skills/jolly/images/` ships product PNGs referenced by the starter recipe — functional, but worth future assets work.
- Forward goal (not built): step-result caching so unchanged verification is not re-run and status returns fast. The logic tier already runs in parallel (`cucumber.js`); caching needs a mechanism + invalidation rules, reporting cache-backed vs fresh, and must never hide a real failure.
- Future plan: extract the AGENTS.md "Scenario-writing rubric" (currently a faked-skill section — concrete-not-abstract, no faux steps, assert the system not the actor, verify invariants per-site, affordances via `pi`) into a standalone reusable scenario-writing skill — project-local first, then upstream to Shipshape — and use it to audit ALL existing scenarios against the rubric in one pass. That audit absorbs the still-open items: distributing per-mutation idempotency out of feature 022, the ~87 faux/weak step defs, and the vague-subject scenarios.
